{
  "name": "docs",
  "version": "2.0.0-alpha.0",
  "private": true,
  "scripts": {
    "build": "vuepress build src --dest dist --temp .temp --cache .cache",
    "clean": "rimraf dist .temp .cache",
    "dev": "vuepress dev src --temp .temp --cache .cache",
    "serve": "serve dist"
  },
  "devDependencies": {
    "@vuepress/bundler-webpack": "^2.0.0-alpha.0",
    "@vuepress/cli": "^2.0.0-alpha.0",
    "@vuepress/core": "^2.0.0-alpha.0",
    "@vuepress/theme-default": "^2.0.0-alpha.0",
    "serve": "^11.3.2"
  }
}
